'''
d) model_trainer.py
Purpose

Builds and trains the machine learning model.

Typical tasks:

Train models
Compare models
Calculate accuracy
Save the best model

Example:

model.fit(X_train, y_train)

Simple meaning:

Creates the final ML model.'''